[package]
name = "macro-cli-demo"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/softwarewrighter/sw-cli"

[dependencies]
sw-cli = { path = "../.." }
clap = "4.0"

[build-dependencies]
sw-cli = { path = "../.." }
